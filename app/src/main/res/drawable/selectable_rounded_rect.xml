<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?selectableDrawableColorDefault">
    <!-- This mask color is not shown, just used as an alpha mask to fill -->
        <shape android:shape="rectangle">
            <solid android:color="@android:color/black"/>
            <corners android:radius="@dimen/selectable_rounded_rect_corner_radius"/>
        </shape>
</selector>